{
  "name": "claude-buddy",
  "version": "1.0.0",
  "private": true,
  "description": "Claude Buddy — a desk-pet that acts as a Bluetooth (Nordic UART) peripheral so Claude Desktop can connect to it. Web UI wrapped with Capacitor; native BLE peripheral on Android.",
  "scripts": {
    "sync": "cap sync",
    "open:android": "cap open android",
    "run:android": "cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
